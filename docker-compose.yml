version: '3.8'
services:
  app:
    image: 577638354424.dkr.ecr.ap-south-1.amazonaws.com/dev/igm-mock-ui:latest  # Will be dynamically updated by Jenkins
    container_name: ${SERVICE_NAME}
    ports:
      - "${PORT}:80"
    environment:
      - NODE_ENV=production
    restart: always